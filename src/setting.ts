interface AppConfig {
  title: string;
  fixedHeader: boolean;
  sidebarLogo: boolean;
}

const config: AppConfig = {
  title: 'N-One Data',
  fixedHeader: false,
  sidebarLogo: false,
};

export default config;
